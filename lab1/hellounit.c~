#include "hellounit.h"

#include <stdio.h>
#include <math.h>

int initialize = 0;


void init(void) __attribute((constructor));

void init()
{
  initialize = 1;
  printf("Module initialized!\n");
  return;
}
void f()
{
assert(initialize == 1);
float b = 7;
	b = sqrt(b);
	printf("Hello World! %f\n", b);
	return;
}

